3.5 Pseudocode

IF Ignition is HIGH:
    IF (Driver is HIGH AND Driver_Belt is LOW) OR (Passenger is HIGH AND Passenger_Belt is LOW):
        Alarm = LOW (Triggered)
    ELSE:
        Alarm = HIGH (Silent)
ELSE:
    Alarm = HIGH (Silent)
