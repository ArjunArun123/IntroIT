3.3 Boolean Expressions (SOP)

To find the Sum of Products, we first find when the alarm is active (Logic 0). However, standard SOP is written for when the output is 1. To make the circuit logic:

    Alarm_Active (Logic 0) = IGN · ((DRIV · NOT BELTD) + (PASS · NOT BELTP)).
